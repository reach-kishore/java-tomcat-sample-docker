version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - echo $CODEBUILD_WEBHOOK_TRIGGER
	  - echo "event is ::: $CODEBUILD_WEBHOOK_EVENT"
      - mvn install
  post_build:
    commands:
      - echo Build completed on `date`